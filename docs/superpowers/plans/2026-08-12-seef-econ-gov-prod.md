# SEEF Plan 13 — Economics, Governance & Production Seams

**Goal:** Close remaining §10.2 deferred items feasible without bare-metal drivers.

**Architecture:** L1 economic params + governance txs in `blockai-execute`; deepen attest/HSM/dataplane seams; `econ_gov_sim` as ops CLI.

## Tasks

1. Types + execute economics/governance  
2. Attest PCRs, HSM ceremony, dataplane backend select  
3. Tests + `econ_gov_sim` + update SEEF §10.2 notes + ship  
